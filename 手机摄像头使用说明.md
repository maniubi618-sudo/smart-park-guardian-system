# 手机摄像头功能使用说明

## 问题说明

浏览器要求在 **HTTPS** 或 **localhost** 环境中才能访问摄像头。如果使用 HTTP 的局域网 IP 访问，摄像头将无法使用。

## 解决方案

我们提供了 **HTTPS 版本**的后端服务来解决这个问题。

---

## 使用步骤

### 1. 启动 HTTPS 后端服务

双击运行：
```
start-backend-https.bat
```

启动后会看到：
```
==================================================
  📷 园区智能安防系统 - HTTPS 启动
==================================================

  💻 API文档 (HTTPS):
     https://localhost:8443/docs

  📱 手机摄像头页面:
     https://10.25.4.228:8443/phone-camera

  ⚠️  首次访问需信任自签名证书
     浏览器提示'不安全' → 点击'继续前往'
==================================================
```

### 2. 启动前端服务

双击运行：
```
start-frontend.bat
```

### 3. PC端操作

1. 访问前端页面：`http://localhost:5173`
2. 登录系统
3. 进入"摄像头管理"页面
4. 点击"手机摄像头"按钮
5. 切换到"手机连接"标签页

### 4. 手机端操作

1. **确保手机和电脑在同一 Wi-Fi 网络**
2. 使用手机浏览器访问显示的连接地址（如：`https://10.25.4.228:8443/phone-camera`）
3. **信任自签名证书**：
   - 浏览器会提示"不安全"或"您的连接不是私密连接"
   - 点击"高级" → "继续前往"（或类似选项）
4. 点击"启用摄像头"
5. 允许浏览器访问摄像头权限

### 5. PC端观看

1. 切换到"手机推流观看"标签页
2. 点击"连接观看"
3. 手机摄像头画面将实时显示！

---

## 文件修改清单

### 新增文件
- `app/main_https.py` - HTTPS 版本的启动文件
- `app/static/phone-camera.html` - 手机端推流页面
- `app/server.crt` - SSL 证书（从 phone-camera-stream 复制）
- `app/server.key` - SSL 密钥（从 phone-camera-stream 复制）
- `start-backend-https.bat` - HTTPS 版本启动脚本
- `手机摄像头使用说明.md` - 本说明文件

### 修改文件
- `app/main.py` - 添加静态文件路由和 CORS 配置
- `app/services/phone_camera_manager.py` - WebSocket 连接管理器
- `app/api/v1/endpoints/camera_router.py` - 手机摄像头路由
- `park-safety-frontend/src/views/cameras/Index.vue` - 手机摄像头对话框

---

## 端口说明

| 服务 | 协议 | 端口 | 说明 |
|------|------|------|------|
| 后端API (HTTP) | HTTP | 8089 | 普通API访问 |
| 后端API (HTTPS) | HTTPS | 8443 | 手机摄像头必须使用 |
| 前端 | HTTP | 5173 | 前端页面访问 |

---

## 常见问题

### Q: 浏览器提示"不安全"怎么办？
A: 这是正常的，因为我们使用的是自签名证书。点击"高级" → "继续前往"即可。

### Q: 手机无法连接？
A: 检查：
1. 手机和电脑是否在同一 Wi-Fi
2. 是否使用了 HTTPS 地址（不是 HTTP）
3. 防火墙是否阻止了 8443 端口

### Q: 还是提示"浏览器不支持摄像头访问"？
A: 确保：
1. 使用 HTTPS 访问
2. 浏览器允许摄像头权限
3. 刷新页面重试

### Q: 如何同时使用普通API和手机摄像头？
A: 可以同时运行两个后端：
- `start-backend.bat` (HTTP 8089) - 普通API
- `start-backend-https.bat` (HTTPS 8443) - 手机摄像头
